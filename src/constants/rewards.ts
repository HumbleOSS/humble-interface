// When true, VOI pair APR estimates include a fixed block-reward uplift (see BLOCK_REWARD_ADJUSTMENT).
export const ENABLE_BLOCK_REWARD_ADJUSTMENT = false;

// Block rewards for VOI pairs (half of nominal per-block value for APR estimates).
export const BLOCK_REWARD_ADJUSTMENT = 18.74 / 2;
